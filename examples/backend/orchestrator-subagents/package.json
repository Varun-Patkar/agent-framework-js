{
  "name": "backend-orchestrator-subagents",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Backend example: a multi-turn orchestrator agent delegating to 2 subagents (a Math specialist backed by calculator MCP, and a Writer specialist). Provider toggle + rich HTML UI.",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/server.ts",
    "start": "tsx --env-file=../../.env src/server.ts"
  },
  "dependencies": {
    "@fastify/static": "^8.0.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "agent-framework-js": "^1.0.0",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
