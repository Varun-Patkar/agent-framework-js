{
  "name": "backend-single-agent-mcp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Backend example: a single-turn agent that uses a calculator MCP server (stdio OR http), with a provider toggle (GitHub Copilot / LM Studio). Serves a rich HTML UI.",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/server.ts",
    "start": "tsx --env-file=../../.env src/server.ts"
  },
  "dependencies": {
    "@fastify/static": "^8.0.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "agent-framework-js": "^1.0.0",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
